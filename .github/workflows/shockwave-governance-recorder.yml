name: Shockwave governance recorder

on:
  push:
    branches:
      - docs/slice-5-shockwave-governance

permissions:
  contents: write

jobs:
  record:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/slice-5-shockwave-governance
          fetch-depth: 0
      - name: Record approved Shockwave governance
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          BASE = "a20d5b14fcd9c02095c427d8d047607bf080cb01"

          def replace_once(path: str, old: str, new: str) -> None:
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f"expected text not found in {path}: {old[:120]!r}")
              p.write_text(text.replace(old, new, 1))

          # README: correct the stale AI-hazard status and name the approved next increment.
          replace_once(
              "README.md",
              "Slice 3 Character Selection & Avatar Ingestion is complete and live accepted. The competitive Grand Prix systems defined for Slice 4 were completed earlier because of a documented sequencing error and remain retained. Slice 5 foundation, visible item boxes, roulette, held-item HUD, desktop/mobile ITEM input, Nitro Surge, Ricochet Kinetic Disc, Homing Seeker Drone, Apex Orbital Missile core, Timed Blast Orb, and Slick Trap are merged, deployed, and live accepted. Nine item effects remain unimplemented, together with real cross-item counter integration, Hyper-Drive Rocket autopilot, full AI tactical item use, the approved-but-not-yet-implemented Blast/Slick AI hazard-response increment, soak/performance evidence, and final Slice 5 acceptance. Issue #106 remains a deferred standings-display defect. Slice 6 remains locked.",
              "Slice 3 Character Selection & Avatar Ingestion is complete and live accepted. The competitive Grand Prix systems defined for Slice 4 were completed earlier because of a documented sequencing error and remain retained. Slice 5 foundation, visible item boxes, roulette, held-item HUD, desktop/mobile ITEM input, Nitro Surge, Ricochet Kinetic Disc, Homing Seeker Drone, Apex Orbital Missile core, Timed Blast Orb, Slick Trap, and the bounded Slick/Blast AI hazard response are merged, deployed, and live accepted. Nine item effects remain unimplemented. The next approved bounded increment is Acoustic Shockwave Pulse under PRD amendment 2.11 / ADR-072; gameplay is not implemented by this governance checkpoint. The remaining open work also includes the other eight item effects, full AI tactical item use, Hyper-Drive Rocket autopilot, final cross-item interaction/counter validation, soak/performance evidence, issue #106, and final Slice 5 acceptance. Slice 6 remains locked."
          )
          replace_once(
              "README.md",
              "- [Approved Slice 5 item-system design and exit checklist](docs/SLICE-5-ITEM-SYSTEM-DESIGN.md)\n",
              "- [Approved Slice 5 item-system design and exit checklist](docs/SLICE-5-ITEM-SYSTEM-DESIGN.md)\n- [Approved Slice 5 Shockwave scope](docs/SLICE-5-SHOCKWAVE-SCOPE.md)\n"
          )

          # Current continuity record: preserve historical evidence below while correcting current authority.
          replace_once(
              "docs/IMPLEMENTATION-STATUS.md",
              "PRD baseline: **v1.1, working implementation amendment 2.10**.",
              "PRD baseline: **v1.1, working implementation amendment 2.11**."
          )
          status_path = Path("docs/IMPLEMENTATION-STATUS.md")
          status = status_path.read_text()
          marker = "PRD baseline: **v1.1, working implementation amendment 2.11**.\n"
          shockwave_status = f"""

## Slice 5 Acoustic Shockwave Pulse governance checkpoint

Manny approved the recommended bounded **Acoustic Shockwave Pulse** scope on 2026-09-07, including the proposed racer push: an outward planar velocity delta that falls linearly from **6 m/s** at the pulse center to **2 m/s** at the governed **5 m** edge. PRD amendment **2.11**, ADR-072, `docs/SLICE-5-SHOCKWAVE-SCOPE.md`, and the Shockwave section in `docs/TESTING.md` are the governing implementation contract.

The approved pulse is instantaneous and centered on the using kart. It pushes eligible unfinished non-owner racers without conventional spinout, destroys ordinary Kinetic Disc / Seeker Drone projectiles inside 5 m, clears Slick Trap / Timed Blast Orb hazards through the accepted queued hazard-clear boundary, and may neutralize only a terminal/dive Apex Missile inside the already accepted 5 m 3D counter radius. Counter resolution must occur before affected projectile/hazard/Apex movement or impact resolution in the same simulation step. Forward/backward ITEM intent is equivalent for Shockwave.

This governance checkpoint changes no gameplay source. It does not enable AI item acquisition/use, change item probabilities, alter racer stats, track/checkpoint authority, accepted Nitro/Kinetic/Seeker/Apex/Blast/Slick behavior outside the new counter interaction, or begin Slice 6. The gameplay implementation is gated until this documentation checkpoint merges to `main` and its post-merge CI/Pages run passes.

Latest verified live checkpoint before this governance branch: `{BASE}`; CI/Pages run `34175582838` passed validation and deployment. Bounded Slick/Blast AI hazard response is LIVE ACCEPTED. Nine item effects remain unimplemented, with Shockwave now the approved next bounded item increment.
"""
          if "## Slice 5 Acoustic Shockwave Pulse governance checkpoint" not in status:
              status = status.replace(marker, marker + shockwave_status, 1)
          status_path.write_text(status)

          replace_once(
              "docs/IMPLEMENTATION-STATUS.md",
              "- Remaining Slice 5 work includes the eleven other unimplemented item effects, real Shockwave/Prismatic and other counter integration, AI item-use policy, interaction/counter validation, soak/performance evidence, deployment, and full-slice live acceptance. Boxes, roulette/HUD/input, Nitro Surge, Kinetic Disc, Seeker Drone, and Apex core are already live accepted.",
              "- Nine item effects remain unimplemented: Acoustic Shockwave Pulse, Blaze Orbs, Frost Orbs, Arc Blade, Arc Hammers, Ink Splat, Nitro Overdrive, Hyper-Drive Rocket, and Prismatic Invincibility. Shockwave is the approved next bounded increment under amendment 2.11 / ADR-072. Full AI item-use policy, final interaction/counter validation, soak/performance evidence, issue #106, deployment evidence for later increments, and full-slice live acceptance remain open. Boxes, roulette/HUD/input, Nitro Surge, Kinetic Disc, Seeker Drone, Apex core, Timed Blast Orb, Slick Trap, and bounded Slick/Blast AI hazard response are already live accepted."
          )
          replace_once(
              "docs/IMPLEMENTATION-STATUS.md",
              "Review the HazardSystem + Timed Blast Orb gameplay PR after its clean-install CI passes, then approve merge/deployment and perform the eight focused checks in `docs/SLICE-5-BLAST-ORB-SCOPE.md`. Preserve accepted Nitro/Kinetic/Seeker/Apex behavior; real Shockwave/Prismatic interactions, AI hazard avoidance and issue #106 remain deferred.",
              "Review and merge the approved Shockwave governance checkpoint after clean-install CI passes. After its post-merge CI/Pages run succeeds, implement only the bounded Acoustic Shockwave Pulse contract in `docs/SLICE-5-SHOCKWAVE-SCOPE.md` on a dedicated gameplay branch, preserving all accepted item behavior and the live-accepted Slick/Blast AI hazard response. Gameplay publication/deployment and live acceptance remain separate gates."
          )
          replace_once(
              "docs/IMPLEMENTATION-STATUS.md",
              "**Slice 5 implementation: IN PROGRESS; BOXES, ROULETTE/HUD/INPUT, NITRO SURGE, KINETIC DISC, SEEKER DRONE, AND APEX CORE LIVE ACCEPTED.**",
              "**Slice 5 implementation: IN PROGRESS; BOXES, ROULETTE/HUD/INPUT, NITRO SURGE, KINETIC DISC, SEEKER DRONE, APEX CORE, TIMED BLAST ORB, SLICK TRAP, AND BOUNDED SLICK/BLAST AI HAZARD RESPONSE LIVE ACCEPTED; SHOCKWAVE GOVERNANCE APPROVED / GAMEPLAY GATED.**"
          )

          # PRD amendment 2.11: approved product/balance fill-in.
          replace_once(
              "docs/PRD.md",
              "Version 1.1 - Final approved baseline; working implementation amendment 2.10",
              "Version 1.1 - Final approved baseline; working implementation amendment 2.11"
          )
          prd_path = Path("docs/PRD.md")
          prd = prd_path.read_text()
          gov_marker = "\n## 1.1 Governance\n"
          amendment = """

## Approved implementation amendment 2.11 - Acoustic Shockwave Pulse counter behavior

Approved September 7, 2026 before Shockwave gameplay implementation. This amendment fills the operating details for the existing Section 15.12 Acoustic Shockwave Pulse without changing its approximately **5 m** radial-defense identity, the fifteen-item probability matrix, one-slot inventory, racer statistics, track/checkpoint authority, accepted item behavior, or the Slice 6 lock. `docs/SLICE-5-SHOCKWAVE-SCOPE.md` and ADR-072 are normative for this bounded increment.

Shockwave is a one-charge instantaneous pulse centered on the using kart. Forward and backward ITEM intent are equivalent. A successful activation consumes its charge when the pulse is committed even if no target is inside the radius. Shockwave itself is not a persistent projectile/hazard and does not reserve a shared item-physics-capacity slot. The readable expanding pressure-ring presentation is gameplay VFX only; final production VFX/audio remains Slice 6.

Eligible unfinished, non-owner, non-immune racers at horizontal center distance **<= 5.0 m** receive no conventional spinout, damage, teleport, or transform snap. Instead the pulse adds one outward planar velocity delta with magnitude linearly interpolated from **6.0 m/s at zero distance to 2.0 m/s at 5.0 m**. A deterministic finite fallback direction is required for coincident centers. The effect may not edit lap/checkpoint/rank/finish authority.

Within the same 5.0 m horizontal radius, ordinary active Ricochet Kinetic Disc and Homing Seeker Drone projectiles are destroyed regardless of their owner-arming state. Slick Trap and Timed Blast Orb hazards are cleared through the accepted queued `HazardSystem` counter boundary. The existing Apex counter remains 3D: only a missile already in its terminal/dive state may be neutralized inside **5.0 m**; rise, sky-travel, and warning/overhead phases are not counterable. Counter queries must resolve before affected projectile/hazard/Apex movement, contact, fuse, or blast processing in that simulation step so a correctly timed pulse wins the frame.

No AI item acquisition/use is enabled by this increment. The player-only `?testItem=shockwave` override and bounded `?testShockwaveCounter=<case>` acceptance fixtures may exercise racer, Kinetic, Seeker, Slick, Blast, and terminal-Apex cases without altering normal distribution or AI tactics. Pause/restart/disposal must leave no queued pulse or presentation state behind. Gameplay implementation, publication/deployment, and live acceptance remain separately gated.
"""
          if "## Approved implementation amendment 2.11 - Acoustic Shockwave Pulse counter behavior" not in prd:
              if gov_marker not in prd:
                  raise SystemExit("PRD governance marker not found")
              prd = prd.replace(gov_marker, amendment + gov_marker, 1)
          prd_path.write_text(prd)

          # Decision record.
          decisions = Path("docs/DECISIONS.md")
          dtext = decisions.read_text()
          adr = """

## ADR-072: Implement Acoustic Shockwave as an instantaneous ordered counter pulse

- **Status:** Approved for implementation; gameplay not yet implemented.
- **Date:** 2026-09-07.
- **Context:** Slice 5 already exposes accepted counter boundaries for terminal Apex, Blast Orb, and Slick Trap. Shockwave is the highest-leverage next item because it closes those real interactions and establishes the ordinary-projectile counter contract before Blaze/Frost/Arc projectiles are added.
- **Decision:** Use one instantaneous 5 m pulse centered on the user. Forward/backward ITEM direction is equivalent. Eligible unfinished non-owner/non-immune racers receive an outward planar velocity delta falling linearly from 6 m/s at the center to 2 m/s at 5 m, with no conventional spinout or transform snap. Kinetic Disc and Seeker Drone projectiles within 5 m are destroyed; Slick/Blast use the existing queued hazard clear; only terminal/dive Apex is counterable within the existing 5 m 3D boundary. Counter resolution precedes affected object movement/contact/fuse/blast in the same simulation step. Shockwave consumes one charge on a committed pulse, uses no persistent shared-capacity slot, and does not enable AI item tactics.
- **Consequences:** Existing accepted item state machines remain authoritative except for their approved removal by a successful Shockwave pulse. Later ordinary projectile items can integrate against one established counter boundary. Final production audio/VFX remains Slice 6.
- **Approval:** Manny approved the recommended Shockwave scope and the 6-to-2 m/s push falloff on 2026-09-07.
- **Gate:** `docs/SLICE-5-SHOCKWAVE-SCOPE.md`, PRD amendment 2.11, `docs/TESTING.md`, and `docs/IMPLEMENTATION-STATUS.md` form the governance checkpoint. Gameplay may begin only after this checkpoint merges and post-merge CI/Pages passes; gameplay publication and live acceptance remain separate approvals.
"""
          if "## ADR-072:" not in dtext:
              decisions.write_text(dtext.rstrip() + adr + "\n")

          # Testing contract.
          testing = Path("docs/TESTING.md")
          ttext = testing.read_text()
          test_section = """

## Slice 5 Acoustic Shockwave Pulse checkpoint

Automated evidence for PRD amendment 2.11 / ADR-072 must confirm:

- one committed Shockwave consumes one charge even when no target is in range; invalid/paused/finished-racer activation cannot create duplicate pulses;
- forward and backward ITEM intent produce the same centered radial pulse;
- horizontal radius boundaries immediately below, at, and above 5.0 m are deterministic;
- eligible racer push adds an outward planar velocity delta that is 6.0 m/s at the center, 4.0 m/s at 2.5 m, and 2.0 m/s at 5.0 m, with a finite deterministic coincident-center fallback and no conventional spinout, teleport, or progress mutation;
- owner, finished-racer, out-of-radius, and generic item-immunity cases are not pushed;
- Kinetic Disc and Seeker Drone objects inside 5.0 m are destroyed before they can move or impact in that simulation step, while objects outside the radius remain active;
- queued Slick Trap and Timed Blast Orb clears resolve before hazard trigger/contact/fuse processing and preserve shared-capacity accounting;
- only terminal/dive Apex inside the existing 5.0 m 3D counter radius is neutralized; rise/sky/warning phases and out-of-radius terminal Apex remain active;
- accepted Nitro/Kinetic/Seeker/Apex/Blast/Slick behavior, probability selection, Slick/Blast AI hazard response, racer stats, checkpoints/laps, camera/driver states, and the 40-object shared capacity remain regression-clean;
- pressure-ring presentation is finite, pause-safe, restart-safe, and disposal-safe; and
- `?testItem=shockwave` plus `?testShockwaveCounter=racer|kinetic|seeker|slick|blast|apex` remain explicit test instrumentation and do not alter normal distribution or enable AI tactics.

Focused deployed desktop/mobile live gate after an approved gameplay merge:

1. Forced Shockwave pickup resolves normally; ITEM produces one readable expanding pressure ring, consumes the held charge, and frees the inventory slot.
2. A nearby rival is pushed outward without a conventional spinout, teleport, wrong-way snap, or race-progress mutation; a rival outside approximately 5 m is unaffected.
3. Incoming Kinetic and Seeker counter fixtures are destroyed by a correctly timed pulse, while a deliberately early/out-of-range pulse does not erase them.
4. Slick and Blast fixtures inside the pulse are cleared without triggering their accepted hostile effect; outside-range hazards remain governed normally.
5. Incoming Apex can be neutralized only during terminal/dive timing inside the 5 m 3D boundary; earlier warning/sky phases cannot be erased.
6. Pause/restart leaves no stuck pulse, warning, hazard, projectile, VFX, inventory, or shared-capacity state.
7. Desktop Shift/E and mobile ITEM activation both work; Brake/Reverse + ITEM remains equivalent rather than directional for Shockwave.
8. Normal unforced gameplay shows no Shockwave test badge/fixture, and accepted Nitro/Kinetic/Seeker/Apex/Blast/Slick plus Slick/Blast AI avoidance remain unchanged.

Record exact gameplay commit, PR CI, post-merge CI/Pages run, desktop/mobile results, defects, and Manny's explicit live acceptance in `docs/IMPLEMENTATION-STATUS.md`. Passing this checkpoint closes only the Shockwave functional/counter increment; the other eight item effects, full AI item policy, final interaction matrix, soak/performance closure, issue #106, overall Slice 5 acceptance, and Slice 6 remain open.
"""
          if "## Slice 5 Acoustic Shockwave Pulse checkpoint" not in ttext:
              testing.write_text(ttext.rstrip() + test_section + "\n")

          # Slice design continuity.
          replace_once(
              "docs/SLICE-5-ITEM-SYSTEM-DESIGN.md",
              "**Governing PRD:** v1.1, working implementation amendment 2.10.",
              "**Governing PRD:** v1.1, working implementation amendment 2.11."
          )
          design = Path("docs/SLICE-5-ITEM-SYSTEM-DESIGN.md")
          stext = design.read_text()
          shockwave_design = """

## Acoustic Shockwave Pulse scope approval - approved 2026-09-07

PRD amendment 2.11 / ADR-072 and `docs/SLICE-5-SHOCKWAVE-SCOPE.md` govern the next bounded item increment. Shockwave remains a one-charge approximately 5 m instantaneous radial defense. The approved fill-ins are a non-spin racer push adding 6-to-2 m/s outward planar velocity falloff, same behavior for forward/backward ITEM intent, ordinary Kinetic/Seeker destruction, queued Slick/Blast clearing, terminal-only Apex neutralization, and counter-before-object-update ordering. Shockwave uses no persistent shared item-physics slot. General AI item acquisition/use remains deferred. The Shockwave checklist item stays incomplete until implementation, hosted validation, deployment, desktop/mobile live checks, and Manny acceptance prove it.
"""
          exit_marker = "\n# Slice 5 exit checklist\n"
          if "## Acoustic Shockwave Pulse scope approval - approved 2026-09-07" not in stext:
              if exit_marker not in stext:
                  raise SystemExit("Slice 5 exit checklist marker not found")
              design.write_text(stext.replace(exit_marker, shockwave_design + exit_marker, 1))

          # New bounded scope document.
          scope = Path("docs/SLICE-5-SHOCKWAVE-SCOPE.md")
          scope.write_text("""# Slice 5 Acoustic Shockwave Pulse Scope

**Status: APPROVED FOR IMPLEMENTATION; GAMEPLAY GATED UNTIL THIS GOVERNANCE CHECKPOINT MERGES AND POST-MERGE CI/PAGES PASSES.**

Baseline `main`: `a20d5b14fcd9c02095c427d8d047607bf080cb01`.

Governing PRD: **v1.1, working implementation amendment 2.11**. Decision: **ADR-072**.

## Purpose

Implement only the existing Acoustic Shockwave Pulse item and the reusable ordered counter boundary it requires. This increment intentionally closes real counter behavior against already accepted Kinetic Disc, Seeker Drone, Slick Trap, Timed Blast Orb, and terminal Apex Missile before later projectile items are added.

It does **not** enable AI item acquisition/use, implement another item, alter probability weights, modify racer stats, change Circuit Alpha/checkpoint/lap authority, reopen accepted item balance, resolve issue #106, complete final Slice 5 soak/performance work, or begin Slice 6.

## Approved gameplay contract

- One charge. A successfully committed pulse consumes the charge even if no target is currently in range and frees the one inventory slot.
- Instantaneous pulse centered on the using kart with **5.0 m** governed radius. Forward/backward ITEM direction is equivalent.
- Shockwave itself is not a persistent projectile/hazard and reserves no slot in the existing shared 40-object item-physics capacity.
- Eligible racer victims are unfinished, non-owner, non-item-immune racers at horizontal center distance `<= 5.0 m`.
- Racer response is **push only**, not conventional spinout: add one outward planar velocity delta, linearly falling from **6.0 m/s at zero distance** to **2.0 m/s at 5.0 m**. Preserve the victim's existing velocity components except for that added planar delta. Do not teleport, snap heading, apply damage, or directly edit race progress.
- Coincident racer centers must use a deterministic finite fallback outward direction so no NaN/zero-normalization path exists.
- Active Ricochet Kinetic Disc and Homing Seeker Drone projectiles inside 5.0 m are destroyed regardless of owner-arming state.
- Slick Trap and Timed Blast Orb inside 5.0 m are cleared through the accepted queued `HazardSystem` boundary.
- Apex countering keeps the already accepted 3D rule: only a missile already in terminal/dive state and within **5.0 m** of the pulse center may be neutralized. Rise, sky travel, and warning/overhead states are unaffected.
- Counter queries resolve before affected projectile/hazard/Apex movement, racer contact, fuse expiry, or blast resolution in the same simulation step. A correctly timed pulse therefore wins that frame.
- The procedural expanding pressure ring is gameplay-readability VFX only. Exact visual easing/duration may be tuned reversibly without changing gameplay radius/timing. Final production VFX/audio belongs to Slice 6.
- Pause prevents gameplay-time progression of any pending presentation/timer state. Restart, return to hub, and disposal clear queued pulse and VFX state.
- No AI item collection, tactical use, or tactical timing is enabled in this increment.

## Implementation boundaries

Prefer a small `ShockwaveSystem` or equivalent item-domain owner that queues one pulse event and presentation without owning racer progress. `KartTimeTrial` may dispatch the pulse into existing systems in a fixed order. `ProjectileSystem` may add a generic queued radial-clear boundary for ordinary projectiles; `HazardSystem.queueClearWithinRadius` and `ApexMissileSystem.queueCounterPulse` remain the accepted hazard/Apex boundaries.

The implementation must not special-case accepted Kinetic/Seeker/Hazard/Apex state machines beyond exposing/removing them through the approved counter interfaces. No direct transform mutation is allowed for racer push. Use physics/controller velocity authority and keep checkpoint/lap/rank code read-only.

## Deterministic acceptance instrumentation

- `?testItem=shockwave` forces only the player's pickup to Shockwave through the existing governed forced-item harness.
- `?testShockwaveCounter=racer|kinetic|seeker|slick|blast|apex` may place exactly one marked bounded counter scenario for the player after race start. These fixtures must use production object/effect rules, must not spend AI inventory, must not enable AI tactical item use, and must reset cleanly.
- A normal URL must never show a Shockwave test badge or force a counter target.

## Automated acceptance gate

1. Charge commit/retention and one-slot release are atomic and deterministic.
2. Forward/backward intents are equivalent.
3. Radius tests cover immediately below, at, and above 5.0 m.
4. Racer push tests cover 6.0 m/s at center, 4.0 m/s at 2.5 m, 2.0 m/s at 5.0 m, coincident-center fallback, owner/finished/immunity exclusion, and no direct progress mutation.
5. Kinetic/Seeker clears occur before their movement/impact and preserve outside-radius objects.
6. Slick/Blast clears occur before trigger/contact/fuse and preserve outside-radius hazards/capacity accounting.
7. Apex clears only terminal/dive objects inside the existing 5 m 3D radius.
8. Pause/restart/disposal/VFX cleanup leaves no queued pulses or leaked resources.
9. Existing Nitro, Kinetic, Seeker, Apex, Blast, Slick, probability, shared-capacity, controller/camera/sprite, AI-race, and Slick/Blast hazard-response regressions remain passing.
10. Normal selector/gameplay remains fixture-free.

## Deployed live gate

1. Forced pickup/use/charge consumption/HUD slot release and pulse tell.
2. In-range racer push versus out-of-range control, with no conventional spinout/teleport/progress jump.
3. Kinetic and Seeker correctly timed counter plus early/out-of-range negative controls.
4. Slick and Blast clear plus out-of-range negative controls.
5. Terminal Apex counter plus non-terminal negative control.
6. Pause/restart cleanup and no shared-capacity leak.
7. Desktop Shift/E and mobile ITEM; reverse modifier remains equivalent for Shockwave.
8. Normal unforced URL plus accepted-item and Slick/Blast AI-hazard regressions.

## Governance / implementation gate

Manny approved this complete scope, including the 6-to-2 m/s racer push falloff, on 2026-09-07. This document, PRD amendment 2.11, ADR-072, `docs/TESTING.md`, `docs/SLICE-5-ITEM-SYSTEM-DESIGN.md`, and `docs/IMPLEMENTATION-STATUS.md` form the governance checkpoint.

Gameplay implementation may begin only after this documentation checkpoint merges to `main` and its post-merge CI/Pages run passes. Gameplay publication/deployment and live acceptance remain later separate gates. No other item effect is authorized by this scope.
""")

          # Remove this temporary recorder from the final branch diff.
          Path(".github/workflows/shockwave-governance-recorder.yml").unlink()
          PY

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md docs/PRD.md docs/DECISIONS.md docs/IMPLEMENTATION-STATUS.md docs/TESTING.md docs/SLICE-5-ITEM-SYSTEM-DESIGN.md docs/SLICE-5-SHOCKWAVE-SCOPE.md .github/workflows/shockwave-governance-recorder.yml
          git diff --cached --check
          git commit -m "Record approved Shockwave governance"
          git push origin HEAD:docs/slice-5-shockwave-governance
